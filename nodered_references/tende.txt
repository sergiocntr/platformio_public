[
    {
        "id": "b02d8d78a022dc67",
        "type": "mqtt out",
        "z": "6e615ab59211c0dc",
        "name": "TendeState",
        "topic": "",
        "qos": "",
        "retain": "",
        "respTopic": "",
        "contentType": "",
        "userProps": "",
        "correl": "",
        "expiry": "",
        "broker": "2a66be145c853355",
        "x": 1530,
        "y": 1080,
        "wires": []
    },
    {
        "id": "e8ce0d0805f0baa4",
        "type": "function",
        "z": "6e615ab59211c0dc",
        "name": "fromTuyaToProcess",
        "func": "\n/* \n * Nodo Funzione Node-RED: Tuya Feedback -> Chrono\n * Questo nodo processa i casi A, B, C che arrivano dal server Tuya\n * Output: verso topic \"homie/tuya/tende/state\"\n */\n\nlet data = msg.payload.data;\nlet deviceName = msg.payload.deviceName;\nif (!data || !deviceName) return null;\n\n// Mappatura nomi -> ID Chrono\nconst nameMap = {\n    \"TENDA_SALOTTO\": 0,\n    \"TENDA_LEO\": 1,\n    \"PERSIANA_SALOTTO\": 2,\n    \"PERSIANA_LEO\": 3,\n    \"PERSIANA_CAMERA\": 4\n};\n\nlet id = nameMap[deviceName];\nif (id === undefined) return null;\n\n// Estraiamo la percentuale (DPS 2 o DPS 10 a seconda del dispositivo)\nlet percentuale = data.dps[\"2\"];\nif (percentuale === undefined) percentuale = data.dps[\"10\"]; // Fallback per alcuni moduli\n\nif (percentuale !== undefined) {\n    msg.payload = { \"topic\": \"tende\",\"t\": id, \"p\": percentuale };\n    return msg;\n}\n\nreturn null;\n",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1060,
        "y": 1080,
        "wires": [
            [
                "1ce865b4ce7d2296"
            ]
        ]
    },
    {
        "id": "7ec8bba1cb0faa28",
        "type": "function",
        "z": "6e615ab59211c0dc",
        "name": "processToBuffer",
        "func": "/* \n * Node-RED Function: Tuya JSON -> Buffer Binario Tende\n * Input: msg.payload = {topic:\"tende\", t:id, p:percentuale} (dal nodo Tuya)\n * Output: msg.payload = Buffer binario per MQTT\n *         msg.topic = \"homie/tuya/tende/binary\" (o come preferisci)\n */\n\n// Costanti\nconst MAGIC = 0xAA;\nconst VERSION = 0x02;\nconst NUM_TENDE = 5;\n\n// Inizializza stato completo nel context\nvar tendeState = context.flow.get(\"tendeState\") || {\n    magic: MAGIC,\n    version: VERSION,\n    num_tende: NUM_TENDE,\n    tende: []\n};\n\n// Inizializza array tende se vuoto\nfor (var i = 0; i < NUM_TENDE; i++) {\n    if (!tendeState.tende[i]) {\n        tendeState.tende[i] = { id: i, percent: 0 };\n    }\n}\n\n// Riceve array di update dal nodo accumulatore\nif (msg.payload && Array.isArray(msg.payload)) {\n    var updates = msg.payload;\n    \n    // Applica tutti gli update\n    updates.forEach(function(update) {\n        if (update.t !== undefined && update.p !== undefined) {\n            var id = update.t;\n            var percent = update.p;\n            \n            if (id >= 0 && id < NUM_TENDE) {\n                tendeState.tende[id].percent = Math.min(100, Math.max(0, percent));\n                node.log(`Aggiornato stato tenda ${id}: ${percent}%`);\n            }\n        }\n    });\n    \n    // Salva stato aggiornato\n    context.flow.set(\"tendeState\", tendeState);\n}\n\n// --- Costruzione buffer binario (come prima) ---\nvar bufferSize = 1 + 1 + 1 + (NUM_TENDE * 3) + 1;\nvar buffer = Buffer.alloc(bufferSize);\nvar offset = 0;\n\n// Header\nbuffer.writeUInt8(tendeState.magic, offset++);\nbuffer.writeUInt8(tendeState.version, offset++);\nbuffer.writeUInt8(tendeState.num_tende, offset++);\n\n// Dati tende (ordinate per ID)\nfor (var i = 0; i < NUM_TENDE; i++) {\n    buffer.writeUInt8(tendeState.tende[i].id, offset++);\n    buffer.writeUInt8(tendeState.tende[i].percent, offset++);\n   \n}\n\n// Checksum\nvar checksum = 0;\nfor (var i = 0; i < buffer.length - 1; i++) {\n    checksum ^= buffer[i];\n}\nbuffer.writeUInt8(checksum, offset);\n\n// Prepara messaggi output\nvar binaryMsg = {\n    topic: \"homie/espNowBridge/buffer\",\n    payload: buffer,\n    retain: true,\n    qos: 1\n};\n\n\nvar debugMsg = {\n    topic: \"homie/tuya/tende/debug\",\n    payload: tendeState,  // Oggetto direttamente, non stringa\n    retain: true\n};\n\n// Invia entrambi\nreturn [[binaryMsg], [debugMsg]];",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1390,
        "y": 1200,
        "wires": [
            [
                "b02d8d78a022dc67",
                "f96b439c8f2b04e3"
            ],
            [
                "2f81c41b0a7654cf"
            ]
        ]
    },
    {
        "id": "1ce865b4ce7d2296",
        "type": "function",
        "z": "6e615ab59211c0dc",
        "name": "Accumulatore stati",
        "func": "// Accumulatore stati tende con debounce\nconst DELAY_MS = 500; // Aspetta 500ms di silenzio prima di processare\nconst NUM_TENDE = 5;\n\n// Inizializza storage nel context\nvar pendingUpdates = context.flow.get(\"pendingUpdates\") || {};\nvar timeoutId = context.flow.get(\"timeoutId\");\n\n// Processa il messaggio in ingresso (dal nodo Tuya)\nif (msg.payload && msg.payload.t !== undefined) {\n    var id = msg.payload.t;\n    var percent = msg.payload.p;\n\n    // Accumula l'update\n    pendingUpdates[id] = {\n        t: id,\n        p: percent,\n        timestamp: Date.now()\n    };\n\n    context.flow.set(\"pendingUpdates\", pendingUpdates);\n\n    // Log per debug\n    node.log(`Accumulato update tenda ${id}: ${percent}%`);\n}\n\n// Se esiste già un timeout, cancellalo (reset del debounce)\nif (timeoutId) {\n    clearTimeout(timeoutId);\n}\n\n// Imposta nuovo timeout\ntimeoutId = setTimeout(function () {\n    // Passa tutti gli update accumulati al prossimo nodo\n    var updates = context.flow.get(\"pendingUpdates\");\n\n    if (Object.keys(updates).length > 0) {\n        // Crea un array di update ordinati per ID\n        var updateArray = [];\n        for (var i = 0; i < NUM_TENDE; i++) {\n            if (updates[i]) {\n                updateArray.push(updates[i]);\n            }\n        }\n\n        // Invia al prossimo nodo\n        var outMsg = {\n            payload: updateArray,\n            updates: updates,\n            timestamp: Date.now()\n        };\n\n        node.send(outMsg);\n\n        // Pulisci gli update processati\n        context.flow.set(\"pendingUpdates\", {});\n        node.log(`Inviato batch di ${updateArray.length} aggiornamenti`);\n    }\n\n    // Pulisci il timeout ID\n    context.flow.set(\"timeoutId\", null);\n\n}, DELAY_MS);\n\n// Salva il nuovo timeout ID\ncontext.flow.set(\"timeoutId\", timeoutId);\n\n// Non inviare nulla ora, aspettiamo il timeout\nreturn null;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1310,
        "y": 960,
        "wires": [
            [
                "7ec8bba1cb0faa28"
            ]
        ]
    },
    {
        "id": "2a66be145c853355",
        "type": "mqtt-broker",
        "name": "mosq1883",
        "broker": "localhost",
        "port": "1883",
        "clientid": "",
        "autoConnect": true,
        "usetls": false,
        "protocolVersion": "4",
        "keepalive": "60",
        "cleansession": true,
        "birthTopic": "",
        "birthQos": "0",
        "birthPayload": "",
        "birthMsg": {},
        "closeTopic": "",
        "closeQos": "0",
        "closePayload": "",
        "closeMsg": {},
        "willTopic": "",
        "willQos": "0",
        "willPayload": "",
        "willMsg": {},
        "userProps": "",
        "sessionExpiry": ""
    }
]